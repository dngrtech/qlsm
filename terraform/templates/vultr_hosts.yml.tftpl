# Ansible inventory fragment for Terraform-managed Vultr servers
# Template variables expected: instance_name, instance_ip, private_key_path

[vultr_servers]
${instance_name} ansible_host=${instance_ip} ansible_user=ansible ansible_ssh_private_key_file=${private_key_path} ansible_port=22